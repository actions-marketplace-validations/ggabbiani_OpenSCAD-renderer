name: Draft Release

on:
  workflow_call: # Permette di essere chiamato da altri workflow

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          draft: true